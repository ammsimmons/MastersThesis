##### Generate a fully crossed design 
load(tidyverse)

O <- 100 #number of objects 
R <- 4 # number of raterse 

sim.rater <- function(mu, sig2) {
  

}


rater.mean <- 10
rater.var <- 10

dat <- tibble(
  subject = c(rep(1:(S/5), each = 4)),
  rater = c(rep(1:R, times = (S/5))

)
datMiss$rater <- , rep(1:R, each = (S/5)))